rules:

  - rule_id: 0
    sender:
      senderName: "A.my_namespace"
      senderType: "service"
    receiver:
      receiverName: "B.*"
      receiverType: "*"
    protocol: http
    resource:
      resourceType: httpPath
      resourceName: "/*"
    operation: GET
    decision: allow
  

