rules:

  - rule_id: 0
    sender: "A.my_namespace"
    receiver: "B.*"
    resource:
      resourceProtocol: http
      resourceType: httpPath
      resourceName: "/*"
    operation: GET
    decision: allow
  

