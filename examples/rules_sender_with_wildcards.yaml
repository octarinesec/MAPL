rules:

  - rule_id: 0
    sender:
      senderName: "A.*"
      senderType: "service"
    receiver:
      receiverName: "B.my_namespace"
      receiverType: "*"
    protocol: http
    resource:
      resourceType: httpPath
      resourceName: "/*"
    operation: GET
    decision: allow
  

